


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    
    <!-- Google Font -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f3f4f6;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
        }
        .card {
            background: #ffffff;
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px;
        }
        .card h2 {
            font-size: 1.75rem;
            font-weight: 600;
            color: #1e293b;
            margin-bottom: 1rem;
        }
        .form-input {
            border: 1px solid #e2e8f0;
            padding: 0.75rem 1rem;
            border-radius: 8px;
            width: 100%;
            margin-bottom: 1rem;
            font-size: 1rem;
            outline: none;
            transition: all 0.3s ease;
        }
        .form-input:focus {
            border-color: #2563eb;
            box-shadow: 0 0 5px rgba(37, 99, 235, 0.3);
        }
        .btn {
            background-color: #2563eb;
            color: #ffffff;
            border: none;
            padding: 0.75rem;
            width: 100%;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 500;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .btn:hover {
            background-color: #1d4ed8;
        }
        .text-sm {
            font-size: 0.875rem;
            color: #6b7280;
            text-align: center;
            margin-top: 1rem;
        }
        .text-sm a {
            color: #2563eb;
            text-decoration: none;
        }
        .text-sm a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="card">
        <h2 class="text-center">Welcome Back</h2>
        <form action="/ragister" method="post" onsubmit="return validateForm()">

            <input 
            type="name" 
            id="name" 
            name="name" 
            class="form-input" 
            placeholder="enter name ">
        <small id="emailError" class="text-red-500 hidden">name</small>

            <input 
                type="email" 
                id="email" 
                name="email" 
                class="form-input" 
                placeholder="Email Address">
            <small id="emailError" class="text-red-500 hidden">Invalid email address.</small>

            <input 
                type="password" 
                id="password" 
                name="password" 
                class="form-input" 
                placeholder="Password">
            <small id="passwordError" class="text-red-500 hidden">Password cannot be empty.</small>

            <button type="submit" class="btn">Register Now</button>
        </form>
        <p class="text-sm">Don't have an account? <a href="/">Login</a></p>
    </div>

   
</body>
</html>
